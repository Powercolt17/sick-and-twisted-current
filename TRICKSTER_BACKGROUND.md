# Trickster background and approved buy menu

The Trickster paid-spin mode uses the supplied seven-second Higgsfield loop at its original 1920×1080 resolution and 24 fps. The copied MP4 is unchanged. Its first frame supplies the motion-disabled/loading fallback.

Paid-mode backgrounds crossfade over 900 ms, including direct switches between All In and Trickster. Both cover the complete scene and ground rail. Character and HUD layout are shared; the approved feathered footing remains at the fixed desktop boot line. Off-screen and inactive videos pause after the fade. Reduced-motion settings select the poster.

The buy menu uses only the supplied illustrations from the artwork atlas, with independent live headings, subtitles, prices and buttons. Compact cards retain clear vertical spacing and smaller typography. Desktop retains the three-column, two-row composition with a 660 px width cap; phones use two columns and 44 px action buttons. The bet control stays inside the visible menu, with scroll position reset on opening. Player-facing naming consistently uses Trickster Spins. The refined cards use restrained grain, inset brass edges and pressed crimson buttons, with a short staggered entrance and pointer-only hover feedback. Feature heroes have separate crops and explicit 3/4/5 scatter labels, avoiding unreadable miniature rows; confirmation reuses the same approved hero art. Reduced-motion settings remove entrance and hover motion.

To fit the hosting asset budget without recompressing the approved video or art, the two 60-second theme WAVs were converted to FLAC. Their decoded PCM hashes match the originals, and both decode in the preview browser as stereo 48 kHz, 60 seconds. Mix gains remain unchanged.

Verification: desktop mode activation and purchase/cancel flow, price changes at $1 and $2 base bets, mobile menu and background at 390×844, identical HUD bounds across Base/Trickster/All In, JavaScript syntax, and all newly introduced local asset references.

The Bounty Booster scatter now carries a live 5× marker and the subtitle “5× BONUS CHANCE.” Each of its 3/4/5-scatter bonus probabilities is exactly five times the corresponding base-spin probability (total 1.744% versus 0.3488%). It still costs 3× base bet; the complete-round calibrator retains the 96.60% booster return target. The new regression check exercises the actual sampler branches and all mode RTP limits.

Blood Money uses a new matte ink-style banknote illustration. Hang ’Em High reuses the approved hanging-outlaw figure and painted gallows timber. The offer now uses a larger close-up with preserved black ink, brighter midtones and more vertical room, making the hat, face and rope legible in the compact card. The timber stays fixed while the cowboy sways gently as a single layer, without body distortion or a canvas render loop. CSS motion pauses on hidden tabs, closed menus and hidden confirmation/grid views, and is disabled with reduced motion. Confirmation shares the same artwork; the price and four-scatter trigger stay separate and readable.

Hell to Pay's offer now reads “START WITH 3 WILD REELS” and “BUY FEATURE.” The approved rider art fills more of the card, with a warmer edge and a clearer caption. Existing ink-fire sprites blend around its edges at 24 fps; the rider silhouette, price and controls remain steady. Motion runs only for the visible menu/confirmation artwork and pauses on closing the dialog, changing to another feature confirmation, hiding the tab, or enabling reduced motion. The poster remains available if motion assets fail to load. All 1,325 supported purchased Hell to Pay outcomes were checked to start with exactly three Wild reels; cost and math are unchanged.
